import { ContratProduitKey } from './contrat-produit-key';

describe('ContratProduitKey', () => {
  it('should create an instance', () => {
    expect(new ContratProduitKey()).toBeTruthy();
  });
});
